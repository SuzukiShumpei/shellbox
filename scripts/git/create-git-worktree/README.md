# Git worktree を作成

- Git worktreeを作成し、作成後にroot dirにあるlocal.propertiesをコピー
- worktree は実行pathと並列位置に `repository名-任意名` で作成（実行pathのdir名 = repository名前提）

## 事前準備

- なし

## 使い方

1. 画面に表示される通りに進めればOK。
2. 既に別worktreeで設定されているbranchを指定した場合は作成に失敗するので注意。
